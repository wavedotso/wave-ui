---
"@waveso/ui": minor
---

**ActionBar** — the button and status strings are now overridable for internationalization via a new `labels` prop (`{ save, saving, reset, saveError }`), merged over the English defaults. The `ActionBarLabels` type is exported. (`message` / `pluralMessage` / `blockedMessage` already covered the messages.)
