---
"@waveso/ui": minor
---

Fixed token where CSS minified blur(0px) is invalid. Import and use src/styles.css for all (storyboard and lib).Token for cursor-clickable wired. Motion recipes for consistent lib transitions. Custom toast namespace tokens.
