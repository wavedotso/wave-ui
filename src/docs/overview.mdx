import { Meta } from '@storybook/addon-docs/blocks';

import { ColorGuide } from './color-guide';

{/*
  Colors / Overview — the color guide, authored once as an MDX docs page.
  Rendering the `ColorGuide` component here (rather than as a story) means the
  article appears exactly once, with no autodocs primary/story echo.
*/}

<Meta title="Colors/Overview" />

<ColorGuide />
